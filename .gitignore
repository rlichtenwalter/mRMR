build/
# Staging prefix used by the `install` CI job and by the same sequence run
# locally (cmake --install build/release --prefix "$PWD/stage").
stage/
.cache/
.claude/
**/.vscode

# NFS temporary files (appear when files are deleted while still open over NFS)
.nfs*

# Environment files
.env
.env.*
!.env.example
